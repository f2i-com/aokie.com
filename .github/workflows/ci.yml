name: CI

# Audit REL-001/C-09: this repo previously had NO CI — the plugin's contract,
# lifecycle, isolation and delivery guarantees were only tested on the dev
# machine. Runs on Windows because that is the only platform the plugin
# ships on (WinUSB radio).
on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  plugin:
    name: aokie-plugin (tests, default features)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cargo cache
        uses: Swatinem/rust-cache@v2

      # Lib + contract-schema + integration tests: the manifest/event/command
      # drift gates (contract.rs), call-session lifecycle, ack-mode outbox,
      # callId guards, endpoint classification, truthful health.
      - name: Tests
        run: cargo test -p aokie-plugin

  plugin-voice:
    name: aokie-plugin (check, voice features)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cargo cache
        uses: Swatinem/rust-cache@v2

      # The voice build compiles a distinct cfg surface (agent, AEC, TTS/STT
      # runtimes) that the default build never touches — check BOTH, always
      # (the repo rule since the barge-in work). No release signing gate here:
      # this is a debug check, not a shippable artifact.
      - name: Check (voice)
        run: cargo check -p aokie-plugin --features voice

  # Audit PKG-001 (best-effort until signing infrastructure exists): a
  # reproducible RELEASE plugin bundle — helper built first so its post-build
  # SHA-256 can be pinned into the plugin (the aokie-dongle release gate),
  # then plugin (voice) + helper + manifest + checksums as one artifact.
  # Signing mirrors the desktop pipeline: applied when the WINDOWS_CERT_*
  # secrets exist, loudly UNSIGNED otherwise. Tag/manual builds only — the
  # test jobs stay the required gate on every push.
  plugin-release:
    name: aokie-plugin release bundle
    runs-on: windows-latest
    needs: [plugin, plugin-voice]
    if: github.ref_type == 'tag' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4

      - name: Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cargo cache
        uses: Swatinem/rust-cache@v2

      # Audit AOK-REL-001: the ONLY correct order is
      #   build helper → SIGN helper → VERIFY signature → hash the SIGNED
      #   bytes → build plugin with that hash pinned → sign plugin → verify →
      #   self-test → package.
      # The old job hashed the helper BEFORE signing, so a signed release
      # shipped a plugin whose embedded pin could never match the signed
      # helper — acknowledged only as a warning. Every verification below is
      # a hard failure; nothing about integrity is warning-only anymore.
      - name: Build + sign helper, pin SIGNED hash, build + sign plugin
        shell: pwsh
        env:
          CERT_B64: ${{ secrets.WINDOWS_CERT_PFX_B64 }}
          CERT_PASS: ${{ secrets.WINDOWS_CERT_PASSWORD }}
        run: |
          cargo build -p aokie-dongle --bin aokie-driver-helper --release
          if ($LASTEXITCODE -ne 0) { throw "helper build failed" }

          $signtool = $null
          $pfx = $null
          if ($env:CERT_B64) {
            $pfx = Join-Path $env:RUNNER_TEMP 'signing.pfx'
            [IO.File]::WriteAllBytes($pfx, [Convert]::FromBase64String($env:CERT_B64))
            $signtool = (Get-ChildItem "${env:ProgramFiles(x86)}\Windows Kits\10\bin\*\x64\signtool.exe" | Select-Object -Last 1).FullName
            & $signtool sign /f $pfx /p $env:CERT_PASS /fd SHA256 /tr http://timestamp.digicert.com /td SHA256 target/release/aokie-driver-helper.exe
            if ($LASTEXITCODE -ne 0) { throw "helper signing failed" }
            & $signtool verify /pa target/release/aokie-driver-helper.exe
            if ($LASTEXITCODE -ne 0) { throw "helper signature verification failed" }
          } else {
            Write-Warning "no signing certificate configured — UNSIGNED release flavour (hash pinning still enforced)"
          }

          # Hash the FINAL helper bytes (post-sign when a cert exists) and
          # only now build the plugin, so the embedded pin always matches
          # what actually ships.
          $sha = (Get-FileHash target/release/aokie-driver-helper.exe -Algorithm SHA256).Hash.ToLower()
          "final helper sha256 = $sha"
          $env:AOKIE_EXPECTED_HELPER_SHA256 = $sha
          cargo build -p aokie-plugin --features voice --release
          if ($LASTEXITCODE -ne 0) { throw "plugin build failed" }

          if ($signtool) {
            & $signtool sign /f $pfx /p $env:CERT_PASS /fd SHA256 /tr http://timestamp.digicert.com /td SHA256 target/release/aokie-plugin.exe
            if ($LASTEXITCODE -ne 0) { throw "plugin signing failed" }
            & $signtool verify /pa target/release/aokie-plugin.exe
            if ($LASTEXITCODE -ne 0) { throw "plugin signature verification failed" }
            Remove-Item $pfx
          }

          # Record the pinned hash for the packaging step's cross-check.
          Set-Content -Encoding ascii pinned-helper-sha.txt $sha

      # Audit AOK-REL-002: a voice release without its runtime is a phone
      # that can't hear or speak — missing assets FAIL the release, and the
      # pin/signature invariants are re-checked on the exact bytes packaged.
      - name: Assemble + verify bundle (fail closed)
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force bundle | Out-Null
          Copy-Item target/release/aokie-plugin.exe bundle/
          Copy-Item target/release/aokie-driver-helper.exe bundle/
          Copy-Item crates/aokie-plugin/manifest.json bundle/
          $dll = Get-ChildItem target/release -Filter 'onnxruntime*.dll' -ErrorAction SilentlyContinue
          if (-not $dll) { throw "AOK-REL-002: voice release requires onnxruntime*.dll beside the plugin exe — none found; the packaged receptionist could not transcribe or speak" }
          $dll | Copy-Item -Destination bundle/

          # Cross-check: the helper bytes being PACKAGED must hash to the pin
          # the plugin was built with — one byte of drift fails the release.
          $pinned = (Get-Content pinned-helper-sha.txt -Raw).Trim()
          $packaged = (Get-FileHash bundle/aokie-driver-helper.exe -Algorithm SHA256).Hash.ToLower()
          if ($pinned -ne $packaged) { throw "helper hash drift: pinned $pinned but packaging $packaged" }

          # Release manifest beside the binaries (AOK-REL-001 acceptance).
          $manifest = [ordered]@{
            builtAt = (Get-Date).ToUniversalTime().ToString('o')
            ref = $env:GITHUB_SHA
            flavour = 'voice'
            signed = [bool]$env:CERT_B64
            helperSha256 = $packaged
            files = @(Get-ChildItem bundle -File | ForEach-Object { [ordered]@{ name = $_.Name; sha256 = (Get-FileHash $_.FullName -Algorithm SHA256).Hash.ToLower() } })
          }
          $manifest | ConvertTo-Json -Depth 4 | Set-Content -Encoding ascii bundle/release-manifest.json
        env:
          CERT_B64: ${{ secrets.WINDOWS_CERT_PFX_B64 }}

      - name: Checksums
        shell: pwsh
        working-directory: bundle
        run: |
          Get-ChildItem -File | Where-Object Name -ne 'SHA256SUMS.txt' |
            Get-FileHash -Algorithm SHA256 |
            ForEach-Object { "{0}  {1}" -f $_.Hash.ToLower(), (Split-Path $_.Path -Leaf) } |
            Set-Content -Encoding ascii SHA256SUMS.txt
          Get-Content SHA256SUMS.txt

      - name: Upload bundle artifact
        uses: actions/upload-artifact@v4
        with:
          name: aokie-plugin-windows
          path: bundle/*
          if-no-files-found: error
          retention-days: 30
